# Name the signing service gets in the DB / API / UI.
SIGNING_SERVICE_NAME=pulp-ui-dev-signing
# Email/UID of the dev GPG key generated for signing. Dev-only, passphrase-less.
GPG_EMAIL=pulp@example.com
